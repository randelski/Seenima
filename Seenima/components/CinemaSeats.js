import React from 'react'
import { View, Text, StyleSheet, Dimensions, FlatList, Animated } from 'react-native'


const { width } = Dimensions.get('window')

const CinemaSeats = ({ data }) => {

    if (data && data.seatStatus)
}